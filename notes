
Problems
1 - Design profile Screen
2 - Last time, CategoryWords we already created, but 'const { words, loading, error } = useSelector((state) => state.favorites);' this line is waiting. Also need to create a backend for this.


Today Will Solve: 
P-3


Next Ideas:
1 - Create Translate Screen





Deepseek

here we wrought CategoriesWords. Can you create this screen for me pls? Before sleeping l wanna solve this problem, then sleep))

onPress={() => navigation.navigate('CategoryWords', { categoryId: item.id, categoryName: item.name })}